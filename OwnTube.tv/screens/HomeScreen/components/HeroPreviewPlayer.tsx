// Native stub — the hero autoplay preview is web-only (the still image is used on
// native). The .web.tsx sibling is the real implementation.
export const HeroPreviewPlayer = (_props: { src: string }) => null;
